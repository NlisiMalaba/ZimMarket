NEXT_PUBLIC_API_URL=http://localhost:8080
# When running the API locally (not Docker), add http://localhost:3001 to Cors:SellerPanelOrigin
# or rely on seller-web Next.js rewrites (default) which proxy /api/v1/* to the API.

# Main customer storefront (www / apex)
NEXT_PUBLIC_CUSTOMER_SITE_URL=http://localhost:3000

# Driver portal subdomain
NEXT_PUBLIC_DRIVER_SITE_URL=http://localhost:3002
